import styled from "styled-components";

export const StyledChips = styled.h2`
  font-size: 16px;
  font-weight: 400;
  line-height: 20px;
  text-align: left;
  color: ${(props) => props.theme.grey};
`;
